#> sgp.cosmetics:kill_effects/particle
# `{particle: minecraft_particle}`
# 
# Customizable particle kill effect

$execute at @e[type=marker,tag=sgp.marker,name="death_reaper"] run particle minecraft:$(particle)