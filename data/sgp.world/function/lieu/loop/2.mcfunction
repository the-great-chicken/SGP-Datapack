#> sgp.world:lieu/loop/2
# `{uuid: string, data: compound}`: the head of sgp:data temp.lieu.list.
# Shard 2 of 8 (generated by the location loop generator; all shards are identical apart
# from their successor). Markers 2, 10, ... run through this file, so its macro cache
# only ever holds their uuids. A marker that vanished since registration simply fails the
# `execute as` and the loop moves on.
$execute as $(uuid) at @s run function sgp.world:lieu/scan/2 with storage sgp:data temp.lieu.list[0].data
data remove storage sgp:data temp.lieu.list[0]
execute if data storage sgp:data temp.lieu.list[0] run function sgp.world:lieu/loop/3 with storage sgp:data temp.lieu.list[0]
