#> sgp.kits:bigger/expiry
# @dummy
# @environment sgp.ci:bigger/expiry
#
# The Tank route preserves an active boost and removes it on its final duration tick.

function sgp.ci:bigger/scenarios/expiry
