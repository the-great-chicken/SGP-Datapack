#> sgp.ci:kit_passives/select
# `{kit: kit id}`
#
# Clear the previous kit and apply only the selected kit's production passive setup.

function sgp.kits:clear
$function sgp.kits:collection/$(kit)/specifics
