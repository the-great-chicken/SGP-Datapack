#> sgp.ci:illusions_movement/log_identity
# `{uuid: entity UUID, entity: entity NBT}`
#
# Emit identity diagnostics for a decoy resolved directly from its stored UUID.

$say Illusion identity check: uuid=$(uuid), entity=$(entity)
