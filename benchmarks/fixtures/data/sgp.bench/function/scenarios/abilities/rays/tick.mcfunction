#> sgp.bench:scenarios/abilities/rays/tick
# `{first: int, last: int, players: int}`
# Production ability tick routing does the workload; record exposure only.

$scoreboard players add #rays_player_ticks sgp.bench $(players)
