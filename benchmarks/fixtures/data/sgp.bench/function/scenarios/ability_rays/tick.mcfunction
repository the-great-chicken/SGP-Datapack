#> sgp.bench:scenarios/ability_rays/tick
# `{first: int, last: int, players: int}`
# The production ability tick does the work; this function only records exposure.

$scoreboard players add #rays_player_ticks sgp.bench $(players)
